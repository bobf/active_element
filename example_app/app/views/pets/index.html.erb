<%= active_element.component.page_title 'Pets' %>

<%= active_element.component.table collection: @pets, fields: [
  :name,
  :owner,
  :date_of_birth,
  [:animal, :json_field, { mapper: ->(record) { '{}' } }],
  :created_at
] %>
